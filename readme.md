Hurghada Real Estate Bot (multilingual). API endpoints:
- POST /chat  { message:string, task?: "consult"|"classify"|"handoff" }
- POST /handoff  { ... } // stub

Set env var OPENAI_API_KEY. Start: `npm install && npm start`
